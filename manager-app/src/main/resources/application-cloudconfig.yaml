server:
  forward-headers-strategy: native
spring:
  application:
    name: selmag-manager-service
  config:
    import: 'optional:configserver:'
  cloud:
    config:
      uri: http://config:config-password@127.0.0.1:8888