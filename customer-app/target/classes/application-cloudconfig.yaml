server:
  port: 8083
spring:
  application:
    name: selmag-customer-service
  config:
    import: 'optional:configserver:'
  cloud:
    config:
      uri: http://config:config-password@127.0.0.1:8888